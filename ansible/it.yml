- name: Install & configure Hashicorp Consul cluster
  hosts: osp
  #become: yes
  gather_facts: yes
  pre_tasks:
    - name: Remove consul agents
      set_fact:
         mount: "{{ ansible_mounts  }}"
    - name: Debug
      templates:
         src: 'free.json'
         dest: /home/free_json
      delefate_to: localhost

